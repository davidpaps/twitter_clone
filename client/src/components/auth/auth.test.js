import React from "react";
import { shallow } from "enzyme";
import Auth from "./auth";

describe("Auth", () => {
  let wrapper;

  // beforeEach(() => (auth = new Auth()));

  it("", () => {
    expect().toEqual();
  });
});
